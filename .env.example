# Database
DATABASE_URL="postgresql://oryn:oryn@localhost:5432/oryn"

# API
API_PORT=4000
WEB_ORIGIN="http://localhost:3000"

# Required for authenticated sessions (use a unique random value >= 32 characters)
AUTH_SECRET="replace-with-a-random-secret-at-least-32-characters"

# Web
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Production: never use the demo seed credentials or the example AUTH_SECRET.
